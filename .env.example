# Exemplo raiz — use também backend/.env.example e frontend/.env.example

# --- API (Hostinger Node App / local) ---
PORT=3001
DB_HOST=localhost
DB_PORT=3306
DB_USER=
DB_PASS=
DB_NAME=pep_vendas
# DATABASE_URL=mysql://user:pass@localhost:3306/pep_vendas
JWT_SECRET=troque-isto
CORS_ORIGIN=http://localhost:3000
COOKIE_SECURE=false

# --- Frontend (build estático) ---
NEXT_PUBLIC_API_URL=http://localhost:3001
51--